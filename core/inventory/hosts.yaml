all:
  hosts:
    awsnode:
      ansible_connection: local
      ansible_user: ubuntu
      ansible_become: true
  children:
    kube_control_plane:
      hosts:
        awsnode:
    kube_node:
      hosts:
        awsnode:
    etcd:
      hosts:
        awsnode:
    k8s_cluster:
      children:
        kube_control_plane:
        kube_node:
    calico_rr:
      hosts: {}
