all:
  hosts:
    inference-xeon:
      ansible_connection: local
      ansible_user: ubuntu
      ansible_become: true
  children:
    kube_control_plane:
      hosts:
        inference-xeon:
    kube_node:
      hosts:
        inference-xeon:
    etcd:
      hosts:
        inference-xeon:
    k8s_cluster:
      children:
        kube_control_plane:
        kube_node:
    calico_rr:
      hosts: {}
